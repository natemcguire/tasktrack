{
  "name": "tasktrack-browser-checks",
  "private": true,
  "scripts": {
    "start": "python3 -m tasktrack serve",
    "test:browser": "node tests/browser.mjs",
    "test:hosted": "node tests/hosted.mjs",
    "dev:cloudflare": "wrangler d1 migrations apply tasktrack-accounts --local && uv run --group cloudflare pywrangler dev --local --ip 127.0.0.1 --port 8787 --local-upstream localhost:8787 --var PUBLIC_URL:http://localhost:8787 --var LOCAL_EMAIL:1",
    "deploy:cloudflare": "python3 cloudflare/deploy.py"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@simplewebauthn/browser": "^13.3.0",
    "@simplewebauthn/server": "^14.0.2",
    "esbuild": "^0.28.2",
    "prettier": "^3.9.6",
    "wrangler": "^4.132.0"
  }
}
